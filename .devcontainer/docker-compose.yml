version: '3.8'

networks:
  default:
    external: true
    name: future-devs
services:
  app:
    hostname: sas-univeristy-classes
    container_name: sas-univeristy-classes
    volumes:
      - ../../:/workspaces
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    command: sleep infinity
